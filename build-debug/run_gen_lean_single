#!/bin/bash

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
BUILD_DIR="${BUILD_DIR:-$(pwd)}"
ARGS=(lean --build-dir "$BUILD_DIR")

if [[ "${EOC_NO_BUILD:-0}" != "0" ]]; then
  ARGS+=(--no-build)
fi

ARGS+=("$1")
shift
ARGS+=("$@")

exec python3 "$SCRIPT_DIR/eoc_driver.py" "${ARGS[@]}"
