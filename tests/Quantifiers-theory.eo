(include "Utils.eo")

(declare-const forall (-> @List Bool Bool))

(declare-const exists (-> @List Bool Bool))

(declare-const witness (-> (T Type :implicit) T Bool T))


; maybe just change to lists

(declare-const @k.QUANTIFIERS_SKOLEMIZE (-> (T Type :implicit) Bool T T))
