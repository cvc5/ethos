
(declare-const and (-> Bool Bool Bool) :left-assoc-nil true)

(define singleton-list ((T Type :implicit) (f (-> T T T)) (a T)) (eo::cons f a (eo::nil f)))

(define test1 () (singleton-list and true) :type Bool)
