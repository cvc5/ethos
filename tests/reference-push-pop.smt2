; not a regression, to be used by reference commands
(set-logic ALL)
(declare-fun x () Int)
(assert (> x 1))
(push 1)
(assert (< x 0))
(check-sat)
(pop 1)
(push 2)
(assert (< x 2))
(check-sat-assuming ((< x 3)))
(pop 2)
(assert (> x 0))
(check-sat)
